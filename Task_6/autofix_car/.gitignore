# Miscellaneous generated files and OS specific files
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history/             # If you use a .history folder for local history
.svn/                 # Subversion related
.swiftpm/             # Swift Package Manager related
migrate_working_dir/

# IntelliJ / Android Studio related
*.iml
*.ipr
*.iws
.idea/

# VS Code related (uncomment if you have sensitive configs here)
#.vscode/

# Flutter/Dart/Pub related generated files and caches
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
**/doc/api/           # Generated API documentation
**/ios/Flutter/.last_build_id # iOS build artifact

# Sensitive data files
.env                  # Crucial for environment variables (API keys, secrets)
android/local.properties # Local Android SDK paths
android/key.properties   # Android signing keystore details - CRITICAL
# For Android Gradle cached/generated files
android/.gradle/
# For iOS/macOS Pods/symlinks
ios/.symlinks/
macos/.symlinks/
ios/Flutter/Generated.xcconfig # Generated by Flutter, can contain local paths

# Firebase related (Optional, depending on your team's preference for client-side keys)
# If you consider firebase.json or firebase_options.dart sensitive *for your specific setup*,
# then uncomment these. Otherwise, they are usually committed.
# firebase.json
# lib/firebase_options.dart

# Symbolication and Obfuscation artifacts (usually large files after build)
app.*.symbols
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug/
/android/app/profile/
/android/app/release/

# Specific files that are often generated locally
# You don't want to ignore the whole android/ or ios/ directories!
# For example, android/app/build/ is already covered by /build/
# but sometimes specific generated files inside android/ can be added here.